from collections import Counter

vezes = int(input())
vazio = input()

for x in range(vezes):
    lista = []
    while True:
        try:
            arvore = input()
            lista = lista.append(arvore)

        except EOFError:
            valores_unicos =
            break
